// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  userId         String           @id @default(uuid())
  email          String           @unique
  hashPassword   String
  bio            String?
  profilePicture String?
  searching      Boolean          @default(false)
  longitude      Float            @default(0)
  latitude       Float            @default(0)
  gender         String
  meetups        Meetup[]
  selectedTags   Tag[] //This is their seleected tags. These are saved but they can change this whenever they start search
  Message        Message[]
  MMQueueElement MMQueueElement[]
  notificationToken String        @default("")
}

<<<<<<< HEAD
model Meetup {
  meetupId   String    @id
  users      User[]
  messages   Message[]
  meetupName String
=======
model Tag {
  tagId      String @id @default(uuid())
  name       String @unique
  numberUsed Int    @default(0)
  users      User[]
}

model Meetup {
  meetupId String    @id @default(uuid())
  users    User[]
  messages Message[]
  name     String
>>>>>>> 7278932afe1323a2575297388751fc8a8765c77b
}

model Message {
  messageId  String   @id @default(uuid())
  message    String
  timeStamp  DateTime
  fromUser   User     @relation(fields: [fromUserId], references: [userId], onDelete: Cascade, onUpdate: Cascade)
  fromUserId String
  meetup     Meetup[]
}

model MMQueueElement {
  MMQueueElementId    String @id @default(uuid()) // For UUID generation
  userId              String
  user                User   @relation(fields: [userId], references: [userId])
  activities          String
  preferenceGender    String
  preferenceMaxRadius Float
  preferenceMinPeople Int
<<<<<<< HEAD
}

model Tag {
  name         String  @id @unique
  numSearching Int     @default(0) //number of people currently searching for this tag. 
  User         User?   @relation(fields: [userUserId], references: [userId])
  userUserId   String?
}
=======
}

// model Activity {
//   activityId     String         @id
//   queueElement   MMQueueElement @relation(fields: [queueElementId], references: [MMQueueElementId])
//   queueElementId String
//   activityName   String
// }
>>>>>>> 7278932afe1323a2575297388751fc8a8765c77b
